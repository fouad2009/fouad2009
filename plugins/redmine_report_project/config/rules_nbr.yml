
# =========================
# Actions / Trackers
# =========================
actions:

  # ---------
  # APS
  # ---------
  aps:
    class: Aps

    DEFAULT:
      state: planned
      field: nbr

    INITIALISATION:
      state: not_started
      field: nbr

    ZONAGE:
      state: study_in_progress
      field: nbr

    POINTAGE:
      state: study_in_progress
      field: nbr

    NOTE_CALCULE:
      state: study_in_progress
      field: nbr

    BOQ:
      state: study_in_progress
      field: nbr

    EN_SIGNATURE:
      state: study_in_progress
      field: nbr

    APS_APPROUVE_DO:
      state: study_in_progress
      field: nbr

    SOUS_RESERVE_DIRA:
      state: study_in_progress
      field: nbr

    RESERVE_DIRA_LEVEE:
      state: study_in_progress
      field: nbr

    APS_APPROUVE_DIRA:
      state: study_completed
      field: nbr

    BESOIN_EXPRIME:
      state: study_completed
      field: nbr

  # ---------
  # APD
  # ---------
  apd:
    class: Apd

    DEFAULT:
      state: planned
      field: capacite_acces_odn_prevue

    INITIALISATION:
      state: not_started
      field: capacite_acces_odn_prevue

    SITE_SURVEY:
      state: study_in_progress
      field: capacite_acces_odn_prevue

    PLAN_SCHEMA:
      state: study_in_progress
      field: capacite_acces_odn_prevue

    DEVIS:
      state: study_in_progress
      field: capacite_acces_odn_prevue

    APD_SIGNE_VALIDE:
      state: study_completed
      field: capacite_acces_odn_prevue

